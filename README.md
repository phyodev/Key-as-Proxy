## Key as Proxy
Thanks me later! for who does not have permissions to install B.P.M  -_-  

### Prerequisities
- ^Python3.10
- pip install poetry (after python installed)
- SOCKS5 configurator pro extension (Chrome, Edge, Firefox)

### Installation
- git clone [repo_addr]
- cd [pj_path]
- source $(poetry env info --path)/Scripts/activate or $(poetry env info --path)/bin/activate
- poetry install

### How to Run?
- source $(poetry env info --path)/Scripts/activate or $(poetry env info --path)/bin/activate
- python app.py